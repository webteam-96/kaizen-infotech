// LazyMotion feature bundle. domMax (not domAnimation) is REQUIRED: the site
// uses layoutId shared-element transitions (blog category pill, testimonials)
// and drag (KaizenValues card) — both live in the layout/drag feature set.
export { domMax as default } from 'framer-motion';
